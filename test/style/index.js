
var expect = require('chai').expect;
var HTMLParser = require('../../lib/html');
var CSSParser = require('../../lib/css');
var styleTree = require('../../lib/style');

describe('style tree', function () {
    it('should style a dom node');

    it('should ignore node not matching a class selector');

    it('should ignore node not matching an id selector');

    it('should style a dom tree with multiple selectors');
});
